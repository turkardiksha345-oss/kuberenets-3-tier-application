apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2   # ✅ Important for EKS
  resources:
    requests:
      storage: 1Gi